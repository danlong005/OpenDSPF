     A*  test23c — a field defined by reference ('R' in position 29), which
     A*  needs the file-level REF/REFFLD keyword to resolve.  Supporting that
     A*  is a decided non-goal, so this is refused rather than given a
     A*  guessed length, which would put the field on screen at the wrong width.
     A                                      REF(CUSMSTL1)
     A          R REFTEST
     A            CUST      R        I 10 25
